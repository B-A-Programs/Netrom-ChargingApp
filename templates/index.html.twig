{% extends "base.html.twig" %}

{% block body %}
    <div class="container-fluid" style="padding: 20px 40px; text-align: center;">
        {{ form(form) }}

        <h1><strong>{{ title }}</strong></h1>
        <hr>
        <div class="row m-1">
            {% for loc in locations %}
                <div class="card col-6 post">
                    <a href="{{ path('location', {'name': loc.name}) }}" style="color: black; text-decoration: none;">
                        <div class="card-body">
                            <h3 class="card-title mb-0"><strong>{{ loc.name }}</strong></h3>
                            <h5 style="color: rgb(63, 59, 59);">City: {{ loc.city }} | Number of stations: {{ loc.getStationsNumber() }} | Price: {{ loc.price }}$/kW</h5>
                        </div>
                    </a>
                </div>
            {% endfor %}
        </div>
    </div>
{% endblock %}

{% block script2 %}
    <script>
        {#document.addEventListener('DOMContentLoaded', function() {#}
        {#    let select = document.getElementById("filter_form_cities");#}
        {#    {% for row in cities %}#}
        {#        {% for city in row %}#}
        {#            let op = document.createElement("option");#}
        {#            op.textContent = {{ city }};#}
        {#            op.value = {{ city }};#}
        {#            select.appendChild(op);#}
        {#        {% endfor %}#}
        {#    {% endfor %}#}
        {#    select.style.class = "red";#}

        {#    document.querySelector("#filter_form").innerHTML = "experiment";#}
        {#});#}
        $(document).ready(function() {
            let select = document.getElementById("filter_form_cities");
            {% for row in cities %}
            {% for city in row %}
            let op = document.createElement("option");
            op.textContent = {{ city }};
            op.value = {{ city }};
            select.appendChild(op);
            {% endfor %}
            {% endfor %}
            select.style.class = "red";

            document.querySelector("#filter_form").innerHTML = "experiment";
        })
    </script>
{% endblock %}